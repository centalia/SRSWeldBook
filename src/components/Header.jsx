import {ReactComponent as IconLogo} from "../assets/logo.svg"

const Header = () => {
    return(
        <header className="">
            <img src=IconLogo={}></img>
        </header>
    );
};

export default Header